local config = readconfig("$0/1", "actor/instance/")
config.effect.positionType = nil
config.effect.adapt = nil
config.transform = {
    scale = {
        x = 0.5,
        y = 0.5
    }
}

return config