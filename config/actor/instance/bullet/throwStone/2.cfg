local config = readconfig("$0/1", "actor/instance/")
config.bullet.length = 300
config.bullet.time = 1000
config.bullet.attack.buff = {
    {
        path = "faint",
        time = 2000
    }
}

return config