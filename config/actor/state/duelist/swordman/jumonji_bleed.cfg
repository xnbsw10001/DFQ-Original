local config = readconfig("$0/jumonji", "actor/state/")
config.attack[2].buff = {
    path = "bleed",
    time = 300,
    attackValue = {
        damageRate = 0.5,
        isPhysical = false
    }
}

return config